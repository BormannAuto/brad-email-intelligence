{
  "_WARNING": "HUMAN VERIFICATION REQUIRED — Field names in this file were generated by AI and MUST be verified against live Zoho Mail API documentation before going live. See: https://www.zoho.com/mail/help/api/",
  "_status": "UNVERIFIED — Do not use as authoritative reference until verified",
  "_last_verified": null,
  "_verified_by": null,

  "auth": {
    "token_endpoint": "https://accounts.zoho.com/oauth/v2/token",
    "grant_type": "refresh_token",
    "required_scopes": ["ZohoMail.messages.ALL"],
    "_verify": "Confirm scope name against https://www.zoho.com/mail/help/api/get-access-token.html"
  },

  "messages": {
    "list_endpoint": "https://mail.zoho.com/api/accounts/{accountId}/messages/view",
    "list_params": {
      "limit": "integer (max 100)",
      "start": "integer (offset)",
      "sortorder": "boolean string ('false' = newest first)",
      "receivedTime": "epoch ms (filter)",
      "folderId": "integer (inbox=1, sent=5 — VERIFY folder IDs against live account)"
    },
    "message_fields": {
      "messageId": "string — internal Zoho message ID",
      "msgId": "string — alternate ID field (may differ from messageId)",
      "threadId": "string — thread grouping ID",
      "headerMessageId": "string — RFC 2822 Message-ID header",
      "subject": "string",
      "fromAddress": "string — format: 'Display Name <email@domain.com>'",
      "toAddress": "string — same format as fromAddress",
      "receivedTime": "epoch ms",
      "sentDateInGMT": "epoch ms (sent folder messages)",
      "content": "string — HTML body",
      "htmlContent": "string — alternate HTML body field (verify which is populated)",
      "hasAttachment": "boolean",
      "folderId": "integer",
      "inReplyTo": "string — RFC 2822 In-Reply-To header",
      "_verify": "Run test fetch and log field names from real response before using"
    }
  },

  "threads": {
    "endpoint": "https://mail.zoho.com/api/accounts/{accountId}/threads/{threadId}",
    "response_structure": {
      "data": {
        "messages": "array of message objects"
      }
    },
    "_verify": "Confirm thread response wraps messages under data.messages"
  },

  "drafts": {
    "create_endpoint": "https://mail.zoho.com/api/accounts/{accountId}/messages",
    "create_payload": {
      "toAddress": "string",
      "subject": "string",
      "content": "string — plain text body",
      "mailFormat": "string — 'plaintext' or 'html'",
      "inReplyTo": "string — optional, RFC 2822 Message-ID",
      "threadId": "string — optional, Zoho thread ID"
    },
    "create_response": {
      "data": {
        "messageId": "string — draft message ID",
        "msgId": "string — alternate ID field"
      }
    },
    "_verify": "Confirm draft creation endpoint and payload fields against Zoho API docs"
  },

  "sent_folder": {
    "folderId": 5,
    "_verify": "CRITICAL — Zoho folder IDs are account-specific. Verify by fetching folder list from the live account. folderId=5 is a common default but NOT guaranteed."
  }
}
