{
  "_WARNING": "HUMAN VERIFICATION REQUIRED — Field names in this file were generated by AI and MUST be verified against live Zoho CRM API documentation before going live. See: https://www.zoho.com/crm/developer/docs/api/v3/",
  "_status": "UNVERIFIED — Do not use as authoritative reference until verified",
  "_last_verified": null,
  "_verified_by": null,

  "auth": {
    "token_endpoint": "https://accounts.zoho.com/oauth/v2/token",
    "grant_type": "refresh_token",
    "required_scopes": ["ZohoCRM.modules.READ", "ZohoCRM.modules.CREATE"],
    "_verify": "Confirm scope names against https://www.zoho.com/crm/developer/docs/api/v3/scopes.html"
  },

  "contacts": {
    "base_url": "https://www.zohoapis.com/crm/v3",
    "search_endpoint": "GET /Contacts/search",
    "search_params": {
      "email": "string — search by email address",
      "fields": "comma-separated field names to return"
    },
    "record_fields": {
      "id": "string — Zoho internal record ID",
      "Full_Name": "string — VERIFY: may be 'Full_Name' or separate 'First_Name'/'Last_Name'",
      "Account_Name": "object or string — may be {id, name} when account is linked",
      "Last_Activity_Time": "ISO datetime string",
      "Description": "string — notes field",
      "_verify": "Run test lookup against live CRM. Field name casing is exact-match required."
    }
  },

  "leads": {
    "search_endpoint": "GET /Leads/search",
    "search_params": {
      "email": "string",
      "fields": "comma-separated field names"
    },
    "record_fields": {
      "id": "string",
      "Name": "string — VERIFY: Leads may use 'Name' instead of 'Full_Name'",
      "Company": "string — company name on Lead record",
      "Last_Activity_Time": "ISO datetime string",
      "Description": "string",
      "_verify": "Confirm Lead module field names differ from Contact module"
    }
  },

  "accounts": {
    "search_endpoint": "GET /Accounts/search",
    "search_params": {
      "criteria": "string — COQL-like filter, e.g. (Website:contains:domain.com)",
      "fields": "comma-separated field names"
    },
    "record_fields": {
      "id": "string",
      "Account_Name": "string — company name",
      "Description": "string",
      "_verify": "Confirm Account record field names and search criteria syntax"
    }
  },

  "notes": {
    "create_endpoint": "POST /Notes",
    "create_payload": {
      "data": [
        {
          "Note_Title": "string",
          "Note_Content": "string",
          "Parent_Id": "string — contact ID to link note to",
          "$se_module": "string — 'Contacts' or 'Leads'"
        }
      ]
    },
    "create_response": {
      "data": "array — non-empty on success",
      "_verify": "Confirm Notes endpoint accepts $se_module for both Contacts and Leads"
    }
  },

  "fill_blanks_policy": {
    "_note": "CRM writes are fill-blanks only per CLAUDE.md locked decision. Never overwrite manually entered field values. Unknown contacts are not auto-created."
  }
}
